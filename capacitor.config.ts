import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Localizador_de_lugares',
  webDir: 'dist'
};

export default config;
